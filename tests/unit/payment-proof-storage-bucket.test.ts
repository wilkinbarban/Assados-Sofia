import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';const sql=readFileSync('supabase/migrations/20260826211000_payment_proof_private_bucket.sql','utf8');describe('private proof bucket',()=>{it('is private and bounded to PDF/PNG without client listing',()=>{expect(sql).toContain("'payment-proofs',false");expect(sql).toContain('application/pdf');expect(sql).toContain('image/png');expect(sql).not.toContain('for select to authenticated')})})
