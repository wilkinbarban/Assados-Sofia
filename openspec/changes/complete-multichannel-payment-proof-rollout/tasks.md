# Tasks: Complete Multichannel Payment-Proof Rollout

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 850–1,250 authored lines across seven bounded units; each unit targets ≤400 lines |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 authority DB → PR 2 authority actions/UI → PR 3 Telegram gate → PR 4 handoff/evidence records |
| Delivery strategy | authorized chained delivery |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

## Guardrails and dependencies

- All implementation work starts only after confirming this checkout is `/home/wilkin/proyectos/Asados-worktrees/client-payment-fix-deploy`, `git rev-parse --show-toplevel` resolves to that worktree, the intended branch is `fix/client-payment-flow-deploy` and the active OpenSpec change is `complete-multichannel-payment-proof-rollout`, and `git status --short` has no unrelated changes; do not edit product code or OpenSpec artifacts outside this change. <!-- sdd-owner: implementation -->
- Keep every authored work unit independently reviewable and at or below ~400 additions plus deletions; the authorized chain strategy is `feature-branch-chain`. If the smallest honest unit still exceeds the budget after one causal split, stop before authoring and request maintainer direction rather than compressing code, tests, or documentation. <!-- sdd-owner: implementation -->
- Preserve strict TDD for code changes: add a focused failing test first (RED), make the smallest contract-preserving change (GREEN), add boundary/race/cross-layer cases (TRIANGULATE), then refactor only within that unit while retaining focused tests (REFACTOR). <!-- sdd-owner: implementation -->
- Never record or commit customer content, phone numbers, tokens, raw payloads, storage URLs, full proof/order/message/provider/delivery identifiers, or configuration values; use aggregate outcomes, time windows, image/migration aliases, gate states, and redacted correlation aliases only. <!-- sdd-owner: implementation -->
- Treat `docs/payment-proof-operations.md`, `apps/web/src/lib/payment-proofs/operational-gates.ts`, `apps/web/src/app/actions/payment-proof-admin.ts`, `apps/web/src/components/operator/PaymentProofAdminPanel.tsx`, `apps/web/src/app/api/webhooks/{telegram,evolution}/`, `supabase/migrations/`, `supabase/tests/`, `tests/unit/`, `ops/supabase/`, and relevant deployment/startup health targets as discovery surfaces; do not modify archived or prior OpenSpec changes. <!-- sdd-owner: implementation -->

## Work unit 1 — Phase A: authority DB/RPC contract (PR 1, target ≤400 lines)

- [x] **RED:** Add failing pgTAP coverage in `supabase/tests/payment_proof_reconciliation.sql` (and focused lease/rejection suites when needed) for active `supervisor` and `admin` success, and active/inactive/stale `vendedor` plus unsupported-role denial of amount confirmation, eligible-order linking, reconcile/approve, and rejection; assert each denial leaves proof, order, payment, linkage, queue/notification, lock, and success-audit state unchanged. <!-- sdd-owner: implementation -->
- [x] **GREEN:** Add one new uniquely timestamped forward migration under `supabase/migrations/` that changes the authoritative reconciliation/rejection RPC/RLS predicate to active supervisor-or-admin only, snapshots immutable actor ID and role-at-action-time for committed permitted outcomes, and preserves exact-cent, same-customer, idempotency, lease, and fence checks; do not rewrite historical migrations or reintroduce seller authority. Denied exceptions MUST remain atomic no-ops, including no success-audit write; durable denial telemetry requires a separately designed boundary because same-transaction inserts roll back with the exception. <!-- sdd-owner: implementation -->
- [x] **TRIANGULATE:** Extend SQL tests for direct RPC calls, stale session/profile activity, foreign/ineligible orders, amount mismatch, concurrent/expired lease or fence conflicts, idempotent repeats, and post-action role changes; verify privileged invalid cases are atomic no-ops and audit meaning survives mutable profile changes. <!-- sdd-owner: implementation -->
- [x] **REFACTOR/verify:** Run the focused Supabase suites through `npm run selfhost:test:db` or the repository-approved targeted pgTAP command, record only pass counts and the new migration filename/checksum alias in the current change evidence, and confirm migration history remains forward-only. <!-- sdd-owner: implementation -->

## Work unit 2 — Phase A: actions and UI authority (PR 2, target ≤400 lines; depends on unit 1)

- [x] **RED:** Add failing Vitest/React coverage in `tests/unit/payment-proof-{reconciliation,leased-amount-confirmation,admin-workflow}.test*` for forged/direct/stale vendedor action submissions and seller UI rendering; require safe `FORBIDDEN` feedback without raw database/provider errors and no reconcile, confirm, link, reject, restore, diagnostics, or replay control. <!-- sdd-owner: implementation -->
- [x] **GREEN:** Update `apps/web/src/app/actions/payment-proof-admin.ts` so privileged operations are gated by active supervisor/admin before UI-level work but continue to rely on the RPC as authority; remove the seller-reconciliation gate semantics from financial authorization, provide only stable safe denials, and restrict sensitive listings/order lookup consistently. <!-- sdd-owner: implementation -->
- [x] **GREEN:** Update `apps/web/src/components/operator/PaymentProofAdminPanel.tsx` and any discovered Atendimento/admin payment-proof entry surface so only active supervisors/admins receive financial, restore, diagnostic, and replay affordances; preserve separately approved read-only seller status only if existing policy permits it. <!-- sdd-owner: implementation -->
- [x] **TRIANGULATE/REFACTOR:** Test positive supervisor/admin flows, active/inactive/stale seller and unsupported-role negatives, direct action bypasses, raw-error suppression, and no UI-only authorization assumption; run focused Vitest then `npm run lint` and retain only sanitized result summaries. <!-- sdd-owner: implementation -->

## Work unit 3 — Phase C: subordinate Telegram startup gate (PR 3, target ≤400 lines; depends on unit 1 acceptance)

- [x] **RED:** Add failing tests in `tests/unit/payment-proof-operational-gates.test.ts`, `tests/unit/payment-proof-operational-gates-diagnostics.test.ts`, and `tests/unit/telegram-payment-proof-intake.test.ts` for `TELEGRAM_PAYMENT_PROOF_INGEST_ENABLED`: missing/malformed/unreadable values, cache/database/runtime override non-authority, process-start immutability, redacted diagnostics, and all four canonical×Telegram truth-table states. <!-- sdd-owner: implementation -->
- [x] **GREEN:** Extend `apps/web/src/lib/payment-proofs/operational-gates.ts` with the allowlisted, default-closed Telegram gate and typed sanitized diagnostic state; preserve startup-only process snapshot behavior and make Telegram eligibility exactly `canonicalIngest.effective && telegramIngest.effective`. <!-- sdd-owner: implementation -->
- [x] **GREEN:** Update `apps/web/src/app/api/webhooks/telegram/route.ts`, `apps/web/src/app/actions/payment-proof-admin.ts`, `apps/web/src/components/operator/PaymentProofAdminPanel.tsx`, and the actual Web Compose/startup configuration target discovered from deployment manifests so the variable is declarative, diagnostics are privileged/redacted, and closed Telegram performs no download or canonical admission. <!-- sdd-owner: implementation -->
- [x] **TRIANGULATE/REFACTOR:** Prove all four gate combinations plus ordinary auth/PDF/dedupe behavior, prove a live environment change remains inert until approved Web recreation, run focused tests and `npm run selfhost:config`, and document the immutable-image/Web-recreation rollback procedure without values. <!-- sdd-owner: implementation -->
- [x] Do not run or schedule a new Telegram production canary: cite the completed sanitized Telegram baseline only, preserve its fixture strictly for evidence, and plan its final targeted purge only after it no longer serves the accepted evidence record. <!-- sdd-owner: implementation -->

## Work unit 4 — Phase A handoff and preflight attestation (PR 4, target ≤300 lines; depends on units 1–3)

- [x] Update `docs/payment-proof-operations.md` and current-change-only evidence/runbook material with completed **Web and Telegram** sanitized baseline evidence, production baseline with canonical intake open and processing, reconciliation, cleanup, privileged replay, and WhatsApp intake closed, release/image and forward-migration aliases, dashboards/health/circuit-breaker checks, escalation owner references, environment symlink ownership/mode/path-alias preflight, declarative gate/Web-recreation sequence, immutable-image rollback, explicit non-actions, and no-repeat instruction for completed Web/Telegram canaries. <!-- sdd-owner: implementation -->
- [x] Add an evidence template under `openspec/changes/complete-multichannel-payment-proof-rollout/` that permits only approved window, role/owner reference, aggregate count/outcome, gate states, image/migration aliases, health/circuit-breaker result, rollback readiness, and redacted correlation aliases; explicitly exclude all PII/secrets/full identifiers. <!-- sdd-owner: implementation -->
- [x] Verify documentation references against the actual startup, Compose, health, migration, test, and rollback discovery targets; run `npm test`, `npm run build`, `npm run selfhost:config`, and the applicable migration verification command, recording sanitized status only. <!-- sdd-owner: implementation -->

## Delegated defect repair — Web outbox message idempotency (bounded repair unit)

- [x] Add forward migration `20260906190000_payment_proof_web_message_idempotency.sql` with nullable text `public.mensagens.external_id` and a non-partial unique arbiter, preserving historical NULL rows. <!-- sdd-owner: implementation -->
- [x] Make Web outbox retries collision-safe with PostgREST conflict-ignore semantics and a post-upsert read of `conversa_id`, `remetente`, `conteudo`, and `url_anexo`; acknowledge only an exact binding, classify divergence as permanent `delivery_conflict`, and keep read failures retryable. <!-- sdd-owner: implementation -->
- [x] Add focused unit/bootstrap coverage plus disposable pgTAP and real PostgREST dispatcher validation for first insert, exact duplicate acknowledgement, divergent collision rejection without false acknowledgement, one-row uniqueness, and historical NULL compatibility. <!-- sdd-owner: implementation -->

## Parent-controlled production gates and evidence (no automatic operation)

- [x] After Phase A code, tests, migration identity, sanitized handoff, closed diagnostics, and rollback readiness are accepted, obtain named authorization for one Evolution window; verify worktree/deployment identity, immutable image alias, intended project-owned environment path/symlink/mode without values, canonical+WhatsApp gates adopted only after approved Web recreation, queue/worker/storage/health/circuit breaker healthy, and then **stop immediately before the real Evolution PDF send for explicit user intervention**. <!-- sdd-owner: parent -->
- [x] After explicit user permission to send, conduct exactly one approved non-sensitive authenticated Evolution PDF E2E (optional safe provider retry only if explicitly approved); attest canonical admission/enqueue/fast acknowledgement/worker progression and duplicate safety, no inline render/OCR/extraction, no legacy `comprovantes` write, and Cloud payment media compatible non-admission; close WhatsApp and/or canonical declaratively, recreate Web, and record sanitized evidence on any stop condition or at window end. <!-- sdd-owner: parent -->
- [x] Do not repeat the completed Telegram canary; after the preserved Telegram fixture is no longer needed for accepted baseline evidence, obtain explicit authorization for and perform one targeted purge of that fixture through approved restricted operations, retaining only required hash/audit tombstone and sanitized outcome evidence. <!-- sdd-owner: parent -->
- [x] Obtain separate authorization and bounded health/rollback window for one non-sensitive quarantine→restore canary; have a named active supervisor/admin perform it, verify exactly one original-channel correction notice, audit and lease/fence/idempotency behavior, and seller restore denial; close the lifecycle gate and recreate Web on completion or immediately for duplicate notice, unauthorized mutation, or degraded health. <!-- sdd-owner: parent -->
- [x] Obtain separate authorization and bounded health/rollback window for one non-sensitive expiry→purge→tombstone canary; verify original bytes and derivatives are deleted, hash/audit tombstone persists, later exact bytes get a generic duplicate result without matching metadata, retries remain bounded, and no resurrection/cross-customer disclosure occurs; close lifecycle capability and recreate Web on any stop condition. <!-- sdd-owner: parent -->
- [x] After lifecycle evidence is accepted, obtain separate replay-gate authorization for one eligible non-sensitive dead letter; a named active supervisor/admin uses one idempotency-key alias and observation window to verify one-or-zero authorized replay effect, repeat-key idempotency, audit eligibility/lease/fence result, read-only diagnostics, seller diagnostics/replay denial, and no automatic follow-on replay; close/recreate Web immediately for any replay stop condition. <!-- sdd-owner: parent -->
- [x] After all implementation units and pre-deployment suites are green, build an immutable candidate from the isolated worktree and execute the owner-authorized final production deployment/test. Verify pre/post service health and restart counts, ready/login responses, redacted effective gates, circuit breaker, and rollback image before exercising only the planned bounded scenarios; immediately close temporary gates and/or roll back the immutable image on any stop condition. Record sanitized evidence only. <!-- sdd-owner: parent -->
- [x] Present the complete sanitized evidence index, including the final production deployment/test, gate diagnostics, reconciliation denial/success results, health/circuit-breaker observations, rollback outcomes, and remaining fixture disposition to the named accountable human; obtain and record a decision for processing and reconciliation (closed, named time-bound window, or ongoing with owner/thresholds/on-call/review date) before push or PR. <!-- sdd-owner: parent -->
- [ ] After the human decision and all evidence are accepted, prepare cohesive causal commits matching work units 1–4 plus evidence-only records, re-run targeted tests, `npm test`, `npm run build`, migration/config verification, and runbook review; verify the worktree identity and sanitized diff, push, and open the PR with scope, evidence index, test summary, gate posture, rollback path, and no PII. <!-- sdd-owner: parent -->
- [ ] Keep the opened PR unmerged and stop until a distinct final explicit authorization identifies the PR or commit and authorizes merge; no canary, test, deployment, or PR state is merge authority. <!-- sdd-owner: parent -->

## Acceptance checklist

- [x] Confirm every implemented authority boundary permits only active supervisor/admin reconciliation operations and proves seller/inactive/stale/unsupported denials without protected mutation or success audit. <!-- sdd-owner: implementation -->
- [x] Confirm Telegram admission is startup-captured, fail-closed, independently controlled yet subordinate to canonical intake, proven by the four-state matrix, and does not rerun its completed production canary. <!-- sdd-owner: implementation -->
- [x] Confirm the handoff/evidence material is sanitized, uses current-change documentation only, describes closed-gate posture and rollback/health windows, and contains no prohibited PII or secrets. <!-- sdd-owner: implementation -->
- [x] Confirm each real operational canary remains manually authorized, bounded, observed, rollback-ready, and closed after its window absent the named permanence decision. <!-- sdd-owner: parent -->
