# Canonical Payment Proof Operations — Implementation Tasks

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1,280 additions + deletions total; each candidate must be re-forecast to ≤400 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 1a → 1b → 2a → 2b → 3a → 3b → 4a → 4b → 5a → 5b |
| Delivery strategy | ask-on-risk (resolved for Candidate 1a) |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

**Resolved delivery path.** The maintainer selected `feature-branch-chain`. Candidate 1a is bounded to its local feature branch and one authorized local work-unit commit. Push, PR publication, merge, production operations, and gate changes remain unauthorized.

## Evidence convention for every implementation candidate

Each task below is one bounded candidate/work unit. Before RED and before candidate freeze, record the approved issue ID and `status:approved`, candidate ID/lineage, clean-current-`main` reproduction result, supersession/conflict audit of open PRs, isolated clean worktree identity, `git diff --stat` forecast (additions + deletions), exact commands/output, scenario, invariant, negative controls, rollback result, and independent read-only validation. Keep gates closed throughout apply and verification. Tests and supporting documentation belong to the same reviewable unit; a reviewable commit boundary is only planned, not authorized.

## Mandatory implementation prechecks

- [x] Before Candidate 1a, verify the approved issue is `status:approved`; reproduce the named SQL failures on clean current `main`; audit open issues/PRs for supersession or conflict; create or reuse a clean isolated worktree/candidate; run `git diff --stat` to record the ≤400-line forecast; and record the RDD issue/candidate lineage and evidence location. Allowed edit surfaces: no source edits; discovery targets `.git`, issue/PR tracker, `supabase/tests/`, and candidate metadata. Dependency: implementation authorization plus maintained approval. Acceptance: all preconditions are evidenced or work stops/narrows; negative controls: unapproved, stale-main, conflicted, dirty, or >400 candidate blocks edits; rollback: discard the isolated candidate without mutating main; verification: `git status --short`, `git rev-parse HEAD`, `git diff --stat`, and the repository-approved issue/PR query; RDD evidence: approved issue status, reproduction transcript, conflict/supersession result, candidate ID, and line forecast. <!-- sdd-owner: implementation -->
- [x] Before Candidate 1a, create a CodeGraph-first impact map for `admit_and_enqueue_payment_proof`, maintenance/alert/replay/lock RPCs, Web/Evolution/Telegram/Cloud routes, Server Actions, Atendimento, worker, gates, migrations, and their tests; then read in full the installed Next.js 16 route and Server Action documents named in `design.md` (`node_modules/next/dist/docs/01-app/02-guides/server-actions.md`, `node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/serverActions.md`, `node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`, and `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`). Allowed edit surfaces: no source edits; discovery targets `.codegraph/`, `AGENTS.md`, the four Next.js documents, affected symbols, and tests. Dependency: prior precheck worktree. Acceptance: map records callers/callees, authoritative boundaries, test locations, and current Next.js 16 constraints; negative controls: do not rely on route/UI guards as database authority or map Cloud as a proof provider; rollback: discard documentation/evidence only; verification: `codegraph explore`/read-only CodeGraph queries plus full-document read evidence; RDD evidence: candidate-bound map and document-read receipt. <!-- sdd-owner: implementation -->

## Phase 1 — SQL green baseline

- [x] **1a. RED → GREEN → TRIANGULATE → REFACTOR: recover repeatable SQL test setup and current expectations.** Allowed edit surfaces: `supabase/tests/payment_proof_operational_metrics.sql`, `supabase/tests/payment_proof_admin_alerts.sql`, `supabase/tests/payment_proof_processing_queue.sql`, `supabase/tests/security_handoff_receipts.sql`, and only test-bootstrap helpers directly included by those suites. Dependency: mandatory prechecks. RED: run `npx supabase test db --local --file supabase/tests/payment_proof_operational_metrics.sql`, `npx supabase test db --local --file supabase/tests/payment_proof_admin_alerts.sql`, `npx supabase test db --local --file supabase/tests/payment_proof_processing_queue.sql`, and `npx supabase test db --local --file supabase/tests/security_handoff_receipts.sql`; classify six metrics, four alerts, one queue-budget failure, and blocked handoff as production defect, obsolete expectation, or non-idempotent bootstrap before edits. GREEN: correct only demonstrated stale signatures/fixtures/idempotent setup without weakening invariant assertions. TRIANGULATE: repeat the bootstrap and each suite against the prepared database, proving TAP assertions execute. REFACTOR: make setup minimal and readable. Acceptance: all classifications and rationale are recorded, duplicate policy setup cannot block TAP, and no intake/reconciliation/gate behavior changes. Negative controls: no historic migration rewrite, no assertion deletion to force green, and no production RPC alteration unless classified in 1b. Rollback: revert this harness-only candidate, preserving diagnosis evidence. Verification: the four commands above, then `npm run supabase:test`; retain TAP output and repeat-bootstrap output. RDD evidence: issue/candidate identity, classification matrix, exact SQL output, ≤400 diff forecast, rollback boundary, and independent read-only candidate validation. <!-- sdd-owner: implementation -->
- [x] **1b. NOT REQUIRED — forward-correct only diagnosed active SQL/RPC defects.** Candidate 1a established that the observed failures were obsolete expectations and non-idempotent test/bootstrap behavior, not an active production SQL/RPC defect. No production migration or RPC correction was created. This unit remains deferred unless new independently executable evidence proves a production defect. Retained evidence: Candidate 1a passed 89/89 TAP assertions after harness-only correction, with commit `ba08f65e93acf1dbf4dc76ce0286e201a8217fbb`. <!-- sdd-owner: implementation -->

## Phase 2 — Canonical async/provider paths

- [x] **2a. RED → GREEN → TRIANGULATE → REFACTOR: establish the atomic canonical admission contract and worker boundary.** Allowed edit surfaces: `apps/web/src/lib/payment-proofs/canonical-intake.ts`, `apps/web/src/lib/payment-proofs/processing-worker.ts`, new forward migrations under `supabase/migrations/` only if required, `tests/unit/payment-proof-intake.test.ts`, `tests/unit/payment-proof-web-canonical-integration.test.ts`, `tests/unit/telegram-payment-proof-intake.test.ts`, and `tests/unit/payment-proof-processing-worker.test.ts`. Dependency: Phase 1 full SQL baseline green. RED: add failing cases for admitted, duplicate, rejected, and enqueue-unavailable outcomes; namespace idempotency by channel; global byte dedupe; and spies proving no inline PDF rendering/OCR/advisory extraction. GREEN: make admission, delivery record/dedupe, and exactly one queue/outbox item transactional; map unavailable to 503 and duplicates to 200. TRIANGULATE: test distinct channel namespace collisions, same-byte cross-channel dedupe, enqueue rollback, and worker claim after admission. REFACTOR: centralize typed outcome mapping without changing authority. Acceptance: accepted Web/Evolution/Telegram inputs return 202 only after atomic admission/enqueue, and no admitted-but-unenqueued state exists. Negative controls: no cross-provider delivery key, synchronous extraction, or adapter-created substitute proof. Rollback: revert contract/migration unit before adapter switch; canonical records remain intact. Verification: `npm run test:unit -- tests/unit/payment-proof-intake.test.ts tests/unit/payment-proof-web-canonical-integration.test.ts tests/unit/telegram-payment-proof-intake.test.ts tests/unit/payment-proof-processing-worker.test.ts` and `npm run supabase:test`; retain outcome matrix and timing/spy evidence. RDD evidence: candidate-bound RED/GREEN outputs, transaction negative controls, forecast, rollback proof, and independent read-only validation. <!-- sdd-owner: implementation -->
- [x] **2b. RED → GREEN → TRIANGULATE → REFACTOR: route provider adapters through canonical admission and enforce Evolution-exclusive authority.** Allowed edit surfaces: `apps/web/src/app/api/webhooks/evolution/route.ts`, `apps/web/src/app/api/webhooks/whatsapp/route.ts`, concrete Web payment-proof route/action discovery targets identified by 2a, concrete Telegram handler discovery targets identified by 2a, `tests/unit/evolution-payment-proof-intake.test.ts`, `tests/unit/evolution-payment-proof-compatibility.test.ts`, `tests/unit/telegram-payment-proof-intake.test.ts`, and route-specific tests discovered from CodeGraph. Dependency: 2a green. RED: fail tests for Evolution direct `public.comprovantes` insertion, each adapter’s typed status mapping/fast acknowledgement, Cloud authenticated payment media non-admission, invalid Cloud authentication rejection, Cloud GET challenge, and Cloud general chat. GREEN: delegate all supported proof paths to 2a’s contract, remove the Evolution legacy fallback, and classify Cloud payment media as `ignored_payment_media` with 200 and no proof/work/legacy record. TRIANGULATE: exercise retries, unavailable enqueueing, chat-only Cloud events, and render/OCR/extraction spies across providers. REFACTOR: share adapter translation only; retain provider authentication before business branching. Acceptance: Evolution is the sole WhatsApp proof authority, Cloud compatibility remains, and routes expose no internals. Negative controls: no legacy fallback emergency path, no Cloud proof admission, and no inline expensive processing. Rollback: revert adapter deployment as one authority unit without reintroducing legacy writes. Verification: `npm run test:unit -- tests/unit/evolution-payment-proof-intake.test.ts tests/unit/evolution-payment-proof-compatibility.test.ts tests/unit/telegram-payment-proof-intake.test.ts tests/unit/payment-proof-web-canonical-integration.test.ts`, then `npm run test:unit`; retain HTTP/outcome and no-write spy evidence. RDD evidence: provider matrix, candidate ID/lineage, exact Vitest output, negative controls, diff forecast, rollback boundary, and independent read-only validation. <!-- sdd-owner: implementation -->

## Phase 3 — Seller reconciliation and Atendimento

- [x] **3a. RED → GREEN → TRIANGULATE → REFACTOR: make reconciliation capability, leases, exact invariants, and audit database-authoritative.** Allowed edit surfaces: new forward migrations in `supabase/migrations/`, reconciliation/lock SQL tests under `supabase/tests/`, `tests/unit/payment-proof-reconciliation.test.ts`, and `tests/unit/payment-proof-order-lock.test.ts`. Dependency: 2b green. RED: create actor/token concurrency and transition matrices for active unassigned seller, inactive seller, unauthorized role, supervisor/admin, foreign customer, ineligible/consumed order, amount mismatch, lock conflict/expiry, invalid state, same-request idempotency, conflicting retry, and role change after event. GREEN: add capability/active checks, opaque proof lease ownership/expiry, atomic exact reconciliation/rejection, and immutable actor-role snapshots in the authoritative transaction. TRIANGULATE: run two-actor lock races and assert all failed paths leave proof/order/payment/linkage unchanged. REFACTOR: isolate reusable SQL fixtures while preserving the matrix. Acceptance: any active `vendedor` may reconcile any eligible customer proof, while restore/purge/inspection/replay stay active-supervisor/admin-only. Negative controls: no role arrays as final authority, no lock stealing, no credentials in audit, and no mutable-profile-derived historic role. Rollback: revert seller-capability/lease exposure as one forward unit; retain append-only audit evidence. Verification: `npx supabase test db --local --file supabase/tests/payment_proof_reconciliation.sql` and `npx supabase test db --local --file supabase/tests/payment_proof_order_lock.sql` when created, plus `npm run test:unit -- tests/unit/payment-proof-reconciliation.test.ts tests/unit/payment-proof-order-lock.test.ts` and `npm run supabase:test`; retain matrix output. RDD evidence: authorization/concurrency matrix, audit immutability proof, candidate forecast, rollback scope, and independent read-only validation. <!-- sdd-owner: implementation -->
- [x] **3b. RED → GREEN → TRIANGULATE → REFACTOR: expose the constrained seller workflow in Server Actions and Atendimento.** Allowed edit surfaces: `apps/web/src/app/actions/payment-proof-admin.ts`, `apps/web/src/components/operator/PaymentProofAdminPanel.tsx`, concrete Atendimento route discovery targets from CodeGraph, `tests/unit/payment-proof-admin-workflow.test.tsx`, `tests/unit/payment-proof-admin-order-column.test.ts`, and `tests/unit/payment-proof-reconciliation.test.ts`. Dependency: 3a green and the required Next.js documents read. RED: model stale/unauthorized client submissions, safe field errors, lock conflict/expiry, invalid transition, foreign/ineligible order, amount mismatch, and missing seller recovery controls. GREEN: re-authenticate and validate Server Action inputs, translate typed RPC results to serializable safe results, refresh only successful mutations, and render status/amount/eligible same-customer orders/lock feedback in Atendimento. TRIANGULATE: test active seller for an unassigned customer, inactive/other roles, and supervisor/admin-only recovery affordances. REFACTOR: remove duplicated client authority while retaining defense-in-depth feedback. Acceptance: permitted seller actions are understandable before submission and forbidden recovery actions are absent or explicitly denied without state change. Negative controls: no webhook-style HTTP result from a Server Action, no raw DB errors, and no stale UI bypass. Rollback: revert action/UI exposure together, preserving database audit/history. Verification: `npm run test:unit -- tests/unit/payment-proof-admin-workflow.test.tsx tests/unit/payment-proof-admin-order-column.test.ts tests/unit/payment-proof-reconciliation.test.ts` and `npm run lint`; retain UI/action outcome evidence. RDD evidence: user-flow inventory and black-box bench/runtime proof where available, candidate-bound Vitest/lint output, negative controls, forecast, rollback, and independent read-only validation. <!-- sdd-owner: implementation -->

## Phase 4 — Observability and replay readiness

- [x] **4a. RED → GREEN → TRIANGULATE → REFACTOR: persist unresolved-dead-letter/alert-failure evidence and privileged dry-read/replay contracts.** Allowed edit surfaces: `apps/web/src/lib/payment-proofs/alert-policy.ts`, `apps/web/src/lib/payment-proofs/alert-delivery.ts`, new forward migrations in `supabase/migrations/`, observability/replay SQL tests under `supabase/tests/`, `tests/unit/payment-proof-alert-policy.test.ts`, `tests/unit/payment-proof-alert-delivery.test.ts`, and `tests/unit/payment-proof-operational-failures.test.ts`. Dependency: 3a green. RED: simulate unresolved dead letters, failed alert delivery, seller diagnostics/replay requests, dry inspection, eligible replay retry, and terminal/locked/ineligible replay. GREEN: persist unresolved count/oldest timestamp and sanitized alert-delivery failure ledger with bounded dedupe/cooldown; implement privileged read-only diagnostics and idempotent, eligible, locked, audited replay RPC semantics. TRIANGULATE: prove no alert-on-alert recursion, at-most-one work item per replay key, rejected requests do not mutate targets, and audit records every outcome. REFACTOR: separate diagnostics reads from mutation paths. Acceptance: recovery is observable and least-privilege without automated replay. Negative controls: no Telegram secrets/raw payload errors, inspection lease/retry/state mutation, seller privilege, or deployment/alert/schedule-triggered replay. Rollback: disable new diagnostic/replay policy/interface without deleting durable evidence or changing intake. Verification: `npm run test:unit -- tests/unit/payment-proof-alert-policy.test.ts tests/unit/payment-proof-alert-delivery.test.ts tests/unit/payment-proof-operational-failures.test.ts`, applicable `npx supabase test db --local --file supabase/tests/payment_proof_admin_alerts.sql`, and `npm run supabase:test`; retain persistence/replay matrix. RDD evidence: causal invariant, exact SQL/Vitest evidence, alert-storm negative proof, candidate forecast, rollback boundary, and independent read-only validation. <!-- sdd-owner: implementation -->
- [x] **4b. RED → GREEN → TRIANGULATE → REFACTOR: add privileged, read-only operational diagnostics and explicit replay action surfaces.** Allowed edit surfaces: concrete diagnostics route/action/component targets discovered by 4a CodeGraph map, `apps/web/src/app/actions/payment-proof-admin.ts` only if it is the existing bounded action surface, and dedicated diagnostics/action tests discovered from those targets. Dependency: 4a green. RED: test authorized supervisor/admin diagnostics, seller denial, redaction, dry-read behavior, and action translation for replay eligibility/duplicate/locked/terminal outcomes. GREEN: implement action/route authentication and shape validation, privileged diagnostic rendering, redacted health signal, and explicit replay request handoff to the authoritative RPC. TRIANGULATE: confirm stale client, repeated request, and unauthorized direct action cannot alter recovery state. REFACTOR: keep diagnostics read and mutation paths visibly separate. Acceptance: diagnostics never claim/retry/replay or increment state, and privileged actors receive safe actionable feedback. Negative controls: no seller inspection/replay, no automatic invocation, and no secret/error-detail exposure. Rollback: remove UI/action exposure independently while retaining 4a records/RPC evidence. Verification: `npm run test:unit -- tests/unit/payment-proof-alert-policy.test.ts tests/unit/payment-proof-alert-delivery.test.ts tests/unit/payment-proof-operational-failures.test.ts <diagnostic-test-paths-from-4a>` and `npm run lint`; retain authorization/redaction/dry-read evidence. RDD evidence: operator-flow inventory, candidate-bound commands/results, negative controls, forecast, rollback, and independent read-only validation. <!-- sdd-owner: implementation -->

## Phase 5 — Durable gates and rollout readiness

- [x] **5a. RED → GREEN → TRIANGULATE → REFACTOR: implement startup-captured fail-closed Compose/environment gate parsing.** Allowed edit surfaces: `apps/web/src/lib/whatsapp/evolution-payment-proof-compatibility.ts`, concrete shared gate parser and Compose/environment declaration targets discovered by CodeGraph, `tests/unit/payment-proof-rollout.test.ts`, and dedicated gate parser tests created under `tests/unit/`. Dependency: 4a green. RED: table-test all documented open literals plus missing, unreadable, malformed, cache-local, dynamic runtime override, and unallowlisted names; add process-start adoption cases. GREEN: allowlist named gates (canonical intake, processing, seller reconciliation, privileged replay, cleanup), parse only documented values to `{ effective, reason }`, read process environment only, and capture effective values at Web start. TRIANGULATE: prove environment changes are inert before recreation and adopted only by a fresh process. REFACTOR: centralize non-secret diagnostics data and remove cache override precedence. Acceptance: every invalid/absent input is closed and cannot enable protected behavior. Negative controls: no cache-local file, database toggle, dynamic override, or raw secret in effective state. Rollback: declaratively close gates and use the approved Web recreation procedure; immutable-image rollback remains for code defects. Verification: `npm run test:unit -- tests/unit/payment-proof-rollout.test.ts <gate-parser-test-path>` and `npm run selfhost:config`; retain parser truth table and process-boundary evidence. RDD evidence: candidate ID, exact Vitest/Compose output, closed-default controls, ≤400 forecast, rollback scope, and independent read-only validation. <!-- sdd-owner: implementation -->
- [x] **5b. RED → GREEN → TRIANGULATE → REFACTOR: publish redacted effective-gate diagnostics and test rollout readiness without executing rollout.** Allowed edit surfaces: diagnostics route/component/runbook documentation targets discovered by 5a, `tests/unit/payment-proof-rollout.test.ts`, dedicated diagnostics tests, and the change-local documentation `openspec/changes/canonical-payment-proof-operations/` excluding historical artifacts. Dependency: 5a green. RED: test redacted authorized diagnostics, closed-by-default deployment completion, and runbook stage entry/stop/rollback coverage. GREEN: document and expose effective state/reason (not raw env), and define closed-baseline → intake → processing → reconciliation → recovery-readiness entry evidence, observations, stop conditions, and declarative close-plus-Web-recreation rollback. TRIANGULATE: simulate valid config after fresh process and deployment with unresolved dead letters while proving no automatic opening/replay. REFACTOR: keep rollout instructions short, operationally explicit, and separate from code behavior. Acceptance: diagnostics distinguish effective state from absent/invalid configuration, every stage is reversible, and gates remain closed after implementation verification. Negative controls: no production gate opening, production replay, restore, purge, maintenance invocation, client-data mutation, or rollout automation. Rollback: remove diagnostics/docs safely; configured gate remains closed. Verification: `npm run test:unit -- tests/unit/payment-proof-rollout.test.ts <gate-diagnostic-test-paths>` and `npm run selfhost:config`; retain runbook review and no-auto-operation evidence. RDD evidence: candidate-bound test/config output, rollout readiness checklist, negative controls, forecast, rollback, and independent read-only validation. <!-- sdd-owner: implementation -->

## Final implementation verification

- [x] **6. Verify the complete closed-gate delivery before sync/archive; do not execute rollout.** Allowed edit surfaces: no product edits; evidence may be recorded only in the active candidate’s approved evidence location. Dependency: 1a–5b complete or explicitly evidenced as not required. Run RED/GREEN/TRIANGULATE/REFACTOR as verification discipline: RED reruns all previously failing SQL and behavior matrices on the final candidate; GREEN runs `npm run supabase:test`, `npm run test:unit`, `npm run lint`, `npm run selfhost:config`, and `npm run build`; TRIANGULATE repeats critical provider, reconciliation concurrency, replay, and gate process-boundary commands from 2a–5b on a clean candidate; REFACTOR removes no assertions or evidence and only reports duplicate verification. Acceptance: baseline, canonical intake, provider boundary, seller authority, audit, observability, replay readiness, and fail-closed gate contracts are green with gates closed. Negative controls: no production operation, no deployment, no gate opening, no replay/restore/purge/maintenance invocation, no client data mutation, and no auto commit/push/PR. Rollback: use each candidate’s bounded rollback; stop and report on any failure. Verification: exact commands listed above plus each phase command; retain command outputs, final `git diff --stat`, candidate ID/lineage, and independent read-only validation. RDD evidence: full candidate-bound verification index, unresolved-authority decisions, reviewable work-unit boundaries, and validation verdict. <!-- sdd-owner: implementation -->

## Deferred parent actions — operational and lifecycle gates

These are intentionally outside apply/verify status and require separate explicit operational authorization after review of implementation evidence.

- [x] Selected `feature-branch-chain`; local candidate branches and commits were authorized separately. No push or PR publication was authorized or performed. <!-- sdd-owner: parent -->
- [x] Recorded candidate-bound independent read-only validation outcomes. RDD is globally disabled because of upstream issue #3930, so delivery remains `disabled/unmanaged` and no native receipt is claimed. <!-- sdd-owner: parent -->
- [x] Decided to keep all six production gates closed. No declarative gate opening or controlled Web recreation was authorized or performed. <!-- sdd-owner: parent -->
- [x] Decided that no production dead-letter replay, restore, purge, maintenance invocation, or client-data mutation is required for this implementation lifecycle. Any future operation requires separate authorization. <!-- sdd-owner: parent -->
- [x] Authorized lifecycle continuation toward formal verification and a later explicit sync/archive decision; rollout execution is not an archive prerequisite. <!-- sdd-owner: parent -->
