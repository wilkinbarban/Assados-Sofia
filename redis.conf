# Redis configuration for the local Evolution API cache.
# Keeping an explicit config removes the default-config startup warning and
# makes persistence behavior intentional.
dir /data
save 60 1
appendonly yes
protected-mode no
