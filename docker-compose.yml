services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: asados-web
    restart: always
    ports:
      - "3020:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production

  evolution-api:
    image: evoapicloud/evolution-api:v2.3.7
    container_name: asados-evolution-api
    restart: always
    ports:
      - "8086:8080"
    environment:
      - SERVER_PORT=8080
      - NPM_CONFIG_UPDATE_NOTIFIER=false
      - SERVER_URL=https://casadeasados.duckdns.org/evolution
      - CORS_ORIGIN=*
      - CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS
      - CORS_CREDENTIALS=true
      - AUTHENTICATION_TYPE=apikey
      - AUTHENTICATION_API_KEY=${EVOLUTION_API_KEY:?set EVOLUTION_API_KEY}
      - LOG_LEVEL=ERROR
      - LOG_BAILEYS=fatal
      - LOG_COLOR=false
      - DATABASE_ENABLED=true
      - DATABASE_PROVIDER=postgresql
      - DATABASE_CONNECTION_URI=postgresql://evolution:${EVOLUTION_DB_PASSWORD:?set EVOLUTION_DB_PASSWORD}@evolution-db:5432/evolution?schema=public
      - DATABASE_SAVE_DATA_INSTANCE=true
      - CACHE_REDIS_ENABLED=true
      - CACHE_REDIS_URI=redis://evolution-redis:6379/0
      - CACHE_REDIS_TTL=604800
      - CACHE_REDIS_PREFIX_KEY=asados_evolution
      - CACHE_REDIS_SAVE_INSTANCES=false
      - CACHE_LOCAL_ENABLED=false
      - WEBHOOK_GLOBAL_ENABLED=true
      - WEBHOOK_GLOBAL_URL=https://casadeasados.duckdns.org/api/webhooks/evolution?webhook_secret=${EVOLUTION_WEBHOOK_SECRET:?set EVOLUTION_WEBHOOK_SECRET}
      - WEBHOOK_GLOBAL_WEBHOOK_BY_EVENTS=false
      - WEBHOOK_EVENTS_MESSAGES_UPSERT=true
      - WEBHOOK_EVENTS_CONNECTION_UPDATE=true
      - WEBHOOK_EVENTS_SEND_MESSAGE=true
      - WEBHOOK_REQUEST_TIMEOUT_MS=60000
      - WEBHOOK_RETRY_MAX_ATTEMPTS=5
    depends_on:
      evolution-db:
        condition: service_started
      evolution-redis:
        condition: service_healthy
    volumes:
      - evolution_store:/evolution/store

  evolution-redis:
    image: redis:7-alpine
    container_name: asados-evolution-redis
    restart: always
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - evolution_redis_data:/data
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro

  evolution-db:
    image: postgres:16-alpine
    container_name: asados-evolution-db
    restart: always
    environment:
      - POSTGRES_USER=evolution
      - POSTGRES_PASSWORD=${EVOLUTION_DB_PASSWORD:?set EVOLUTION_DB_PASSWORD}
      - POSTGRES_DB=evolution
    volumes:
      - evolution_db_data:/var/lib/postgresql/data

volumes:
  evolution_store:
  evolution_db_data:
  evolution_redis_data:
# Add auto-activation environment to evolution-api
